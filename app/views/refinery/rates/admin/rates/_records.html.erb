<%= render 'refinery/admin/search_header', :url => refinery.rates_admin_rates_path %>
<div class='pagination_container'>
  <% if @rates.any? %>
    <%= render 'rate_tables' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
